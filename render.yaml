services:
  - type: web
    name: wav-converter-backend
    # Use Render's native Python environment, which includes Node.js
    env: python 
    plan: free
    region: oregon 
    rootDir: server
    # These commands will now work because both Python and Node are in the environment
    buildCommand: "npm install"
    startCommand: "npm start"